{
  "name": "postcss-test",
  "version": "0.0.1",
  "devDependencies": {
    "autoprefixer": "^6.0.3",
    "postcss-cli": "^2.1.0",
    "postcss-partial-import": "^1.3.0"
  },
  "scripts": {
    "postcss": "postcss -c 'postcss.json'"
  }
}
